#include <iostream>
#include "cmath"
using namespace std;
#define f(nq) (((2 * (nq)) / pow(2 + (nq), 2)) * (1 / pow((nq) + 1, 2)))
#define f1 123123123
void test(int a, char b) {
  cin >> a >> b >> c >> d;
  cout << b;
}
int main() {
  cout << "start " << endl;
  if (a) {
    n--;
  } else if (b) {
    n++;
  }
  cout << "stop " << endl;
}
