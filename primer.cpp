#include <iostream>
#define maxi(a, b) ((a) > (b)) ? (a) : (b)
using namespace std;
int main() {
  switch (СССС) {
    case 0:
      y = sin(k / 3) + sqrt(cbrt(k + 1));
      z = sin(k / 3) + sqrt(cbrt(k + 1));
      break;
    case 155:
      y = sin(k / 3) + sqrt(cbrt(k + 1));
      z = sin(k / 3) + sqrt(cbrt(k + 1));
      break;
  }
  return 0;
}