#include <iostream>
#define maxi(a, b) ((a) > (b)) ? (a) : (b)
using namespace std;
int main() {
  double x{}, y{}, z{};
  cout << "Введите x y z:" << endl;
  cin >> x >> y >> z;
  if (qwe) {
    c++;
    a++;
  } else if (qwe2) {
    switch (СССС) {
      case 0:
        y = sin(k / 3) + sqrt(cbrt(k + 1));
        y = sin(k / 3) + sqrt(cbrt(k + 1));
        break;
      default:
        cout << "Can't calculate" << endl;
    }
  }
  cout << "x:<< ";
  return 0;
}