#include <iostream>
#define maxi(a, b) ((a) > (b)) ? (a) : (b)
using namespace std;
int main() {
  double x{}, y{}, z{};
  cout << "Введите x y z:" << endl;
  cin >> x >> y >> z;
  switch (c) {
    case 0:
      y = sin(k / 3) + sqrt(cbrt(k + 1));
      y = sin(k / 3) + sqrt(cbrt(k + 1));
      break;
    case 1:
      y = tan(pow(k, 2)) + sqrt(k + 1);
      break;
    case 2:
      y = pow(atan(k + 1), 2);
      break;
    case 3:
      y = pow(E, (k + 1) / 10);
      break;
    default:
      cout << "Can't calculate" << endl;
  }
  cout << "x:<< ";
  return 0;
}